import { UnwrapTagDirective } from './unwrap-tag.directive';

describe('UnwrapTagDirective', () => {
  it('should create an instance', () => {
    const directive = new UnwrapTagDirective();
    expect(directive).toBeTruthy();
  });
});
